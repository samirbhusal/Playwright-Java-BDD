Feature: User Registration
  As a client of the reqres.in API
  I want to register a user via POST /api/register

  @API-001
  Scenario: Successful registration for a known/defined user
    Given API environment is set up
